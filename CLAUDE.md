# Working agreements

- After each component is completed and its tests pass, commit and push to
  `origin/main` on GitHub without asking for confirmation first. One commit
  per component (matches how this project has been built so far — see git
  history for the pattern: scaffolding, then Policy Tool, then LangGraph
  orchestration, each as its own commit).
- Repo: https://github.com/velmaachyuth2002/enterprise-healthcare-knowledge-agent (public).
